- hosts: 127.0.0.1
  gather_facts: true
  vars:
    versions:
      binutils: '2.37'
      gcc: '11.2.0'
      mpfr: '4.1.0'
      gmp: '6.2.1'
      mpc: '1.2.1'
      linux: '5.13.12'
      glibc: '2.34'
    vcpus: '{{ ansible_processor_vcpus | default(2) }}'
    host_architecture: '{{ ansible_architecture }}'
  roles:
    - name: binutils-pass1
      vars:
        version: "{{ versions.binutils }}"
    - name: gcc-pass1
      vars:
        version: "{{ versions.gcc }}"
        internal_library_version:
          mpfr: "{{ versions.mpfr }}"
          gmp: "{{ versions.gmp }}"
          mpc: "{{ versions.mpc }}"
    - name: linux
      vars:
        version: "{{ versions.linux }}"
    - name: glibc
      vars:
        version: "{{ versions.glibc }}"
        pass1_gcc_version: "{{ versions.gcc }}"
